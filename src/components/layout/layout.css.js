import styled from 'styled-components';

export const LayoutPaint = styled.div`
  background-image: linear-gradient(to top, lightgreen, #9cf, #fff);
  min-height: 100vh;
  min-width: 100vw;
  line-height: 4.2vh;
`;
